export default function AddDriverButton() {
  return (
    <button
      style={{
        padding: "12px 24px",
        background: "#2563eb",
        color: "white",
        border: "none",
        borderRadius: "8px",
        cursor: "pointer",
        fontWeight: "bold",
      }}
    >
      + Add Driver
    </button>
  );
}